MINI_SUMMARY_PROMPT = """
Ты преобразуешь фрагмент транскрипта в мини-конспект.
Извлеки 3-7 ключевых пунктов, которые передают смысл фрагмента без воды.
Каждый пункт должен быть коротким и самодостаточным.
Не добавляй вступления, выводы или объяснения вне списка.
Верни только валидный JSON строго в следующем формате:
{"key_points":["point 1","point 2","point 3"]}
""".strip()

CHUNK_ANALYZE_PROMPT = """
Ты анализируешь один чанк транскрипта урока для endpoint `chunk-analyze` и должен вернуть только валидный JSON.

Пиши по-русски. Не добавляй никаких пояснений, markdown или текста вне JSON.

Что нужно извлечь:
- `key_points`: 3-7 коротких самодостаточных тезисов, которые передают смысл фрагмента.
- `teacher_questions`: только явные вопросы преподавателя.
- `student_answers`: только уверенные ответы или реплики студентов.
- `examples_and_analogies`: только реальные примеры, аналогии, сравнения, метафоры или истории.
- `lesson_events`: только заметные события или смены этапов урока.
- `goals_and_summary`: отметь `intro` и `summary` только если они действительно есть.
- `flags`: точные цитаты с грубостью, неуважительным или чересчур фамильярным тоном.

Общие правила:
- Используй только то, что есть в транскрипте.
- Не выдумывай факты, события или смысл.
- Если секция не найдена, верни пустой список.
- Для всех фрагментов используй реальные `start_ms` и `end_ms`.
- Если реплика разбита на несколько строк, объединяй её в один логичный фрагмент.
- Для `teacher_questions` и `student_answers` перефразируй смысл связно и коротко, если дословная цитата не нужна.
- Для `examples_and_analogies` перефразируй смысл фрагмента, а не копируй его дословно.
- Не больше 10 фрагментов в каждой списковой секции.

Правила по секциям:
1. FIND
- Сначала отбирай только явные и уверенные совпадения.
- Спорные случаи пропускай.
- `student_answers` отмечай только если точно понятно, что это ответ студента.
- `teacher_questions` отмечай только если это явно вопрос преподавателя.
- `examples_and_analogies` отмечай только если это действительно пример, аналогия, сравнение, метафора или история.
- `lesson_events` отмечай только если это конкретное событие урока, а не обычная реплика.

2. FORMULATE
- После отбора собери компактный JSON.
- `key_points` формулируй коротко, без воды, как мини-конспект.
- Для `teacher_questions` укажи `question_type` на английском: `rhetorical`, `checking_understanding`, `factual` или `other`.
- Для `examples_and_analogies` укажи `type` на английском: `example`, `analogy`, `metaphor` или `storytelling`.
- Для `goals_and_summary.intro.present=true` используй только случаи, когда преподаватель вводит тему, обозначает цель или начинает новый блок.
- Для `goals_and_summary.summary.present=true` используй только случаи, когда преподаватель подводит итог, завершает объяснение или суммирует материал.
- Если `present=false`, то `start_ms` должен быть `null`, а `comment` пустым.
- Для `lesson_events` используй короткие событийные формулировки, а не дословные реплики.
- Для `flags.profanity` и `flags.overly_familiar_tone` добавляй только точные фразы из транскрипта.

3. SELF-CHECK
- Каждый фрагмент явно соответствует своей секции.
- Нет дублей, пустых заглушек и сомнительных фрагментов.
- Все поля соответствуют схеме.

Верни JSON строго в таком виде:
{
  "chunk_id": 1,
  "start_time": "00:00",
  "end_time": "00:15",
  "key_points": ["..."],
  "teacher_questions": [
    {
      "start_ms": 4200,
      "end_ms": 9800,
      "question_type": "checking_understanding",
      "text": "..."
    }
  ],
  "student_answers": [
    {
      "start_ms": 184000,
      "end_ms": 187500,
      "text": "..."
    }
  ],
  "examples_and_analogies": [
    {
      "start_ms": 355000,
      "end_ms": 363000,
      "type": "analogy",
      "text": "..."
    }
  ],
  "lesson_events": [
    {
      "start_ms": 0,
      "title": "...",
      "description": "..."
    }
  ],
  "goals_and_summary": {
    "intro": {
      "present": true,
      "start_ms": 0,
      "comment": "..."
    },
    "summary": {
      "present": false,
      "start_ms": null,
      "comment": ""
    }
  },
  "flags": {
    "profanity": [],
    "overly_familiar_tone": []
  }
}
""".strip()

TEACHER_ANALYSIS_PROMPT = CHUNK_ANALYZE_PROMPT

TEACHER_ANALYSIS_AGGREGATE_PROMPT = """
Ты анализируешь результаты анализа чанков транскрипта урока.

Тебе НЕ дан исходный транскрипт.
Тебе даны только результаты анализа чанков.

Твоя задача — собрать итоговый анализ речи преподавателя для интерфейса аналитики.

Пиши по-русски.

Используй только предоставленные данные.
Не придумывай новые цитаты, события или выводы без подтверждения.
Если данных недостаточно — используй осторожные формулировки.

Рабочий процесс:
1. FIND
- Сначала собери только подтверждённые сигналы из чанков по каждой секции.
- Если сигнал спорный, не используй его.
- Не смешивай роли и не переноси фрагменты между секциями.

2. FORMULATE
- После отбора собери итоговый JSON.
- Комментарии должны опираться на конкретные наблюдаемые признаки.
- `teacher_recommendation` формулируй только из уже подтверждённых наблюдений, без новых догадок.
- Учитывай `key_points` как краткое резюме каждого чанка.
- Для `questions_to_students`, `student_answers` и `examples_and_analogies` укажи `start_ms` и `end_ms` у каждого фрагмента.
- Для `examples_and_analogies` перефразируй смысл фрагментов в связный текст и укажи английский `type`.

Входные данные имеют вид:

json
{
  "chunk_analyses": [
    { "...": "..." },
    { "...": "..." },
    { "...": "..." }
  ]
}

Правила агрегации:

1. `lesson_format`
- Определи общий формат занятия по накопленным событиям, вопросам, объяснениям и структуре.
- В `comment` подробно объясни, на основании каких признаков сделан вывод.
- Укажи, какие сигналы перевесили: количество объяснений, типы вопросов, наличие примеров, работа со слайдами, практические этапы, обсуждение ответов и т.д.

2. `audience_engagement.questions_to_students`
- Собери наиболее показательные вопросы преподавателя.
- Максимум 10 фрагментов.
- Удали дубликаты.
- `title` не обязателен: если общий заголовок не нужен, не добавляй его вообще.
- В `comment` укажи, какие типы вопросов преобладают: уточняющие, на понимание, на поиск ответа, на рассуждение, риторические, проверочные.
- Не теряй `question_type` при переносе фрагментов из чанков.
- Комментарий должен опираться на реальные фрагменты, а не на общие слова.
- Не включай вопросы, если это только похожая по форме реплика без явного вопросительного смысла.

3. `audience_engagement.student_answers`
- Собери наиболее характерные реакции преподавателя на студентов.
- Максимум 10 фрагментов.
- `title` не обязателен: если заголовок не нужен, можно не добавлять его в JSON.
- Отмечай только те ответы, где видно, что это действительно реплика студента.
- Если есть сомнение, не включай фрагмент.
- Не включай короткие нейтральные реплики, если нельзя уверенно сказать, что это ответ студента.

4. `lesson_structure.step_by_step_explanation.timeline`
- Восстанови примерную последовательность занятия.
- Отсортируй по времени.
- Максимум 10 элементов.
- События должны фиксировать именно смену этапа или действие на уроке, а не обычные реплики.
- Примеры подходящих элементов:
  - "Введение темы"
  - "Переход к презентации"
  - "Разбор примера"
  - "Проверка ответов"
  - "Начало практики"
  - "Подведение итога"

5. `lesson_structure.goals_and_summary`
- Определи наличие введения и завершения.
- Если `present=true`, добавь короткий `comment`.

6. `material_explanation.examples_and_analogies`
- Собери только реальные примеры, аналогии, сравнения, метафоры и сторителлинг.
- Не добавляй общие фразы вроде "давайте посмотрим" или "разберём это".
- Если один пример или история разбиты на несколько последовательных реплик, объедини их в один цельный фрагмент.
- Переформулируй фрагмент как связный текст, не копируя его дословно.
- Для каждого фрагмента укажи `type` на английском: `example`, `analogy`, `metaphor` или `storytelling`.
- Максимум 10 фрагментов.
- Не включай пограничные случаи, если нельзя уверенно сказать, что это именно пример, аналогия, метафора или история.

7. `teacher_recommendation`
- Добавь отдельный блок с рекомендацией преподавателю.
- Рекомендация должна быть конкретной и применимой на следующем занятии.
- Опирайся только на подтверждённые наблюдения из анализа.

8. `flags`
- Объедини данные из всех чанков.
- Удали дубликаты.
- Выставь `present=true`, если найден хотя бы один подтверждённый случай.

Верни JSON строго в этой структуре:
{
  "lesson_format": {
    "format": str,
    "comment": str
  },
  "audience_engagement": {
    "questions_to_students": {
      "comment": str,
      "fragments": [
        {
          "start_ms": 4200,
          "end_ms": 9800,
          "question_type": "checking_understanding",
          "text": str
        }
      ]
    },
    "student_answers": {
      "comment": str,
      "fragments": [
        {
          "start_ms": 184000,
          "end_ms": 187500,
          "text": str
        }
      ]
    }
  },
  "lesson_structure": {
    "step_by_step_explanation": {
      "timeline": [
        {
          "start_ms": 0,
          "title": "Введение",
          "description": str
        },
        {
          "start_ms": 320000,
          "title": "Переход к презентации",
          "description": str
        }
      ]
    },
    "goals_and_summary": {
      "intro": {
        "present": true,
        "start_ms": 0,
        "comment": str
      },
      "summary": {
        "present": false,
        "start_ms": null,
        "comment": ""
      }
    }
  },
  "material_explanation": {
    "examples_and_analogies": {
      "fragments": [
        {
          "start_ms": 355000,
          "end_ms": 363000,
          "type": "analogy",
          "text": str
        }
      ]
    }
  },
  "teacher_recommendation": {
    "title": "Рекомендация преподавателю",
    "comment": str
  },
  "flags": {
    "profanity": {
      "present": false,
      "fragments": []
    },
    "overly_familiar_tone": {
      "present": true,
      "fragments": [
        {
          "start_ms": 903000,
          "end_ms": 905500,
          "text": str
        }
      ]
    }
  }
}

Self-check before returning JSON:
- Все комментарии содержат только подтверждённые наблюдения.
- В каждой секции нет сомнительных или спорных фрагментов.
- `student_answers` включает только явно студенческие реплики.
- `teacher_recommendation` не добавляет новых фактов, которых нет в анализе.
- `examples_and_analogies` содержит перефразированные фрагменты и корректный `type`.
- Для каждого фрагмента заполнены `start_ms` и `end_ms`.
- Не больше 10 фрагментов в каждой секции.
- Нет дублей и пустых заглушек.
""".strip()

LESSON_SUMMARY_PROMPT = """
Ты эксперт по методике обучения и предметный тьютор.
Создай красивый, структурированный и образовательный конспект урока для студента.

На входе будут `key_points` или фрагменты транскрипта.
Используй их как основу, но активно улучшай материал:
- логично перестраивай идеи
- добавляй объяснения
- добавляй примеры
- добавляй мини-выводы
- добавляй практические замечания
- преобразуй неформальные формулы в корректный LaTeX
- пиши формулы только если это занятие по математике или физике или в уроке четко произносят формулы
- не добавляй формулы если урок с ними не связан

Пиши по-русски.

ОЧЕНЬ ВАЖНЫЕ ПРАВИЛА ФОРМАТИРОВАНИЯ:
- Верни только валидный JSON.
- Не пиши ничего вне JSON.
- `content` поддерживает Markdown:
  - **жирный**
  - маркированные списки
  - нумерованные списки
  - таблицы Markdown
  - формулы LaTeX через $...$ или $$...$$
- Активно используй экранированные переводы строк `\\n`.
- Не пиши один большой абзац.
- Каждый блок должен выглядеть как настоящая учебная заметка: заголовки, списки, примеры, таблицы при необходимости.
- Если тема содержит формулы, перепиши ВСЕ формулы в корректный LaTeX, даже если во входе они записаны обычным текстом.
- Если во входе встречается что-то вроде "x squared", "a/b", "sqrt x", "integral from 0 to 1", преобразуй это в LaTeX.
- Используй таблицы, если есть сравнение, классификация, формулы или типичные ошибки.
- Сгенерируй 3-5 подробных блоков.

СТРУКТУРА КОНТЕНТА:
Каждый блок обычно должен включать:
1. Краткое объяснение идеи
2. Основные правила или алгоритм
3. Пример или мини-демонстрацию
4. Типичную ошибку или практическое замечание

Верни строго этот JSON:
{
  "summary": [
    {
      "subtopic": "Название подтемы",
      "content": "Markdown content with \\n, lists, tables and LaTeX"
    }
  ]
}
""".strip()

QUIZ_PROMPT = """
Ты специалист по учебному дизайну. Твоя задача — создать тест для студента на основе материалов урока.
Используй и теоретические вопросы по конспекту, и практические задания на решение или анализ.
Практические задания придумывай сам — они должны отличаться от упражнений в материалах урока.

Правила:
- `question_text`, `options` и `explanation` поддерживают встроенный LaTeX (`$...$`).
- Для `multiple_choice` индекс `correct_answer` начинается с нуля.
- Для `multiple_choice` пояснение должно быть понятным студенту.

Типы вопросов:

multiple_choice
{
    "question_id": int,
    "question_text": "Найдите значение выражения $x^2$ при $x=3$.",
    "question_type": "multiple_choice",
    "options": ["3", "6", "9", "12"],
    "correct_answer": 2,
    "explanation": "Подставим x = 3: \\n$3^2 = 9$.",
    "subtopic": "str"
}

open_ended
{
    "question_id": int,
    "question_text": "Объясните, что такое интеграл.",
    "question_type": "open_ended",
    "options": null,
    "correct_answer": "str",
    "explanation": "",
    "subtopic": "str"
}

Верни только валидный JSON:
{"quiz": [{"question_id": int, "question_text": str, "question_type": "multiple_choice" | "open_ended", "options": [] | null, "correct_answer": int | str, "explanation": str, "subtopic": str}]}
""".strip()

GRADE_PROMPT = """
Ты оцениваешь ответ студента.
Оцени ответ как 0 или 1.
Верни только валидный JSON в следующем формате:
{"scores":[{"question_id": int, "score": int}, ...]}.
""".strip()

PRACTICE_SUMMARY_PROMPT = """
Ты эксперт-тьютор и специалист по учебному дизайну.
Создай адаптивный практический конспект на основе слабых подтем и ошибок студента.

Пиши по-русски.

Цель:
Помочь студенту понять, что пошло не так, как это исправить и как решать похожие задания в следующий раз.

ОЧЕНЬ ВАЖНЫЕ ПРАВИЛА ФОРМАТИРОВАНИЯ:
- Верни только валидный JSON.
- Не пиши ничего вне JSON.
- `content` поддерживает Markdown:
  - **жирный**
  - маркированные списки
  - нумерованные списки
  - таблицы Markdown
  - формулы LaTeX через $...$ или $$...$$
- Активно используй экранированные переводы строк `\\n`.
- Не пиши один большой абзац.
- Используй таблицы для ошибок, сравнений, формул или шаблонов исправления.
- Преобразуй все неформальные формулы в корректный LaTeX.
- Если во входе есть формулы в обычном тексте, перепиши их через LaTeX.

Для каждой слабой подтемы включи:
1. Что именно вызвало трудности у студента
2. Правильное правило или идея
3. Пошаговый алгоритм решения
4. Пример
5. Типичную ошибку и как её избежать
6. Короткий совет для тренировки

Верни строго этот JSON:
{
  "summary": [
    {
      "subtopic": "str",
      "content": "Markdown content with \\n, lists, tables and LaTeX"
    }
  ]
}
""".strip()

TEACHER_AGGREGATE_PART_STRUCTURE_PROMPT = """
Ты анализируешь результаты анализа чанков транскрипта урока (lesson_format, lesson_structure, teacher_recommendation).
Тебе НЕ дан исходный транскрипт. Тебе даны только результаты анализа чанков.
Твоя задача — собрать итоговый анализ структуры и формата занятия.

Пиши по-русски.
Используй только предоставленные данные.

Верни JSON строго в этой структуре:
{
  "lesson_format": {
    "format": str,
    "comment": str
  },
  "lesson_structure": {
    "step_by_step_explanation": {
      "timeline": [
        {
          "start_ms": int,
          "title": str,
          "description": str
        }
      ]
    },
    "goals_and_summary": {
      "intro": { "present": bool, "start_ms": int, "comment": str },
      "summary": { "present": bool, "start_ms": int, "comment": str }
    }
  },
  "teacher_recommendation": {
    "title": "Рекомендация преподавателю",
    "comment": str
  }
}
""".strip()

TEACHER_AGGREGATE_PART_ENGAGEMENT_PROMPT = """
Ты анализируешь результаты анализа чанков транскрипта урока (audience_engagement, material_explanation).
Тебе НЕ дан исходный транскрипт. Тебе даны только результаты анализа чанков.
Твоя задача — собрать итоговый анализ вовлеченности и подачи материала.

Пиши по-русски.
Используй только предоставленные данные.

Верни JSON строго в этой структуре:
{
  "audience_engagement": {
    "questions_to_students": {
      "comment": str,
      "fragments": [
        {
          "start_ms": int,
          "end_ms": int,
          "question_type": str,
          "text": str
        }
      ]
    },
    "student_answers": {
      "comment": str,
      "fragments": [
        {
          "start_ms": int,
          "end_ms": int,
          "text": str
        }
      ]
    }
  },
  "material_explanation": {
    "examples_and_analogies": {
      "fragments": [
        {
          "start_ms": int,
          "end_ms": int,
          "type": str,
          "text": str
        }
      ]
    }
  }
}
""".strip()

TEACHER_AGGREGATE_PART_FLAGS_PROMPT = """
Ты анализируешь результаты анализа чанков транскрипта урока (flags).
Тебе НЕ дан исходный транскрипт. Тебе даны только результаты анализа чанков.
Твоя задача — собрать итоговый список флагов (грубость, фамильярность).

Пиши по-русски.
Используй только предоставленные данные.

Верни JSON строго в этой структуре:
{
  "flags": {
    "profanity": {
      "present": bool,
      "fragments": [{ "start_ms": int, "end_ms": int, "text": str }]
    },
    "overly_familiar_tone": {
      "present": bool,
      "fragments": [{ "start_ms": int, "end_ms": int, "text": str }]
    }
  }
}
""".strip()
